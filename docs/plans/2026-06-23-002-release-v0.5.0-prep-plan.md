---
title: "release: v0.5.0 正式版發布準備 — 版本對齊 + CHANGELOG + README + 程式碼優化"
type: release
status: completed
date: 2026-06-23
claims: {}  # 發布準備計畫，無需 claims 追蹤
---

# release: v0.5.0 正式版發布準備

## Overview

目標：將 **`refactor/webui-api-v1` 分支**（當前 `pyproject.toml` 誤寫為 `0.6.0`）整理成正式的 **v0.5.0** 發布版本。

**結論先行**：這是一次發布準備（release polish），不是功能開發。主要工作是**版本號對齊、CHANGELOG 合併、README 補齊、測試驗證、整理 git tag**。程式碼本身已功能完整——Vue 3 SPA、`/api/v1` REST 後端、SDK runtime 都已就位。

## 當前狀態 vs 目標

| 面向 | 當前 | 目標 |
|---|---|---|
| `pyproject.toml` version | `0.6.0` | `0.5.0` |
| CHANGELOG.md | 0.5.0（6/18）+ 0.6.0（6/23）各自獨立 | 合併成一個完整的 v0.5.0 版本條目 |
| `v0.5.0` git tag | 指向 6/18 舊 commit | 刪掉重打，指向 refactor/webui-api-v1 HEAD |
| README.md | 687 行，涵蓋 CLI 管線但缺 SPA/API v1/SDK | 補齊前端+API+SDK 說明 |
| README.zh.md | 412 行，同步同上 | 同步更新 |
| 測試套件 | ~616 個測試檔 | 全數通過 + 無 regression |
| 前端 | Vue 3 SPA（零建構 ESM） | 確認可正常 dev server |

## Tasks

### T1: 版本號對齊 + git tag 整理

**動作：**
1. `pyproject.toml` `version = "0.6.0"` → `"0.5.0"`
2. 刪除本地 `v0.5.0` tag：`git tag -d v0.5.0`
3. 如果 remote 有 tag，也刪：`git push origin --delete v0.5.0`
4. 在當前分支 HEAD 打新 tag：`git tag v0.5.0`

**驗證：** `git tag -l v0.5.0` 指向當前 commit，`grep version pyproject.toml` 輸出 `0.5.0`

### T2: CHANGELOG 合併整理

**當前問題：**
- CHANGELOG 有兩個版本條目：0.5.0（2026-06-18）和 0.6.0（2026-06-23）
- 因為你是主動降版本（0.6.0 → 0.5.0），需要把兩個版本的變更合併成一個 v0.5.0 條目

**動作：**
1. 將 [0.6.0] section 的內容（Vue 3 SPA、API v1、SDK、design system、perf lanes）移到 [0.5.0] section 下
2. 按照 Added / Changed / Fixed 分類合併排列（不要空洞）
3. 更新 version compare links（`[Unreleased]` link、`[0.5.0]` link）
4. 日期統一為發布日

**驗證：** CHANGELOG 中不再出現 `0.6.0` 字樣，只有一個完整的 v0.5.0 條目

### T3: README.md 更新

**當前：** README 以 CLI 管線為核心，缺少 Vue 3 SPA / API v1 的完整說明。

**需新增/更新內容：**
1. **WebUI section** — 從 `python webui.py` 開始，說明 SPA routes（`/app/settings`, `/app/monitor`, `/app/history`, `/app/sites`, `/app/profiles`）
2. **API v1 reference** — 簡表列出 `/api/v1/` endpoints（pipeline、llm、site settings、credentials、monitor、history、profiles）
3. **SDK usage** — `PipelineAPI`、`publish_inprocess()` 範例
4. **Frontend architecture** — 零建構 ESM、TanStack Query、Pinia、CSS tokens
5. **Project structure** 目錄樹更新 — 加入 `frontend/`、`openapi/`（如果存在）
6. **Adapter table** — 確認 20+ 平台數量正確

**不修改的範圍：** CLI 管線說明、SEO anchor keywords、輸出契約、退出碼——這些沒有變化。

### T4: README.zh.md 同步更新

與 T3 相同的內容，翻譯為中文。保持與 README.md 的結構一致。

### T5: 測試驗證

**動作：**
1. 跑 `make coverage` 或 `pytest tests/ -q --timeout=30` 確認全數通過
2. 如果有失敗的測試，分析並修復（僅修 regression，不修既有問題）
3. 確認 `make lint`（ruff）沒有新錯誤

**不做的範圍：**
- 不修改測試本身（除非要修 CI regression）
- 不提高 coverage threshold

### T6: 最後檢查

- `git status` clean
- 所有變更檔案確認無遺漏
- `python webui.py` 能正常啟動
- 前端 `npm run dev` / `vite` 能正常啟動

## 不做（明確排除）

- 不改 `AGENTS.md`（它是活的專案指南，不屬於版本發布的一部分）
- 不改 `docs/solutions/` 或 `docs/plans/` 中的其他文件
- 不重構程式碼架構
- 不增加新功能
- 不調整 CI workflow

## Out of Scope

- 發布到 PyPI（這步你決定是否要做）
- 建立 GitHub Release
- 合併到 `main` 分支（你決定何時 merge）

---

## Verification

1. `pyproject.toml` 版本 = `0.5.0`
2. CHANGELOG 無 `0.6.0` 殘留，只有合併後的完整 v0.5.0
3. README 涵蓋 SPA / API v1 / SDK
4. 所有測試 pass
5. `v0.5.0` tag 指向當前 HEAD
6. `git status` clean
