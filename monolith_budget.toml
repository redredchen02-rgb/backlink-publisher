# Monolith SLOC budget. See:
# - docs/plans/2026-05-18-006-feat-monolith-sloc-ceiling-plan.md
# - docs/brainstorms/2026-05-18-monolith-loc-ceiling-requirements.md
#
# Schema (enforced by tests/test_no_monolith_regrowth.py):
#   each [files."<path>"] entry MUST have:
#     ceiling  -- int, radon SLOC ceiling
#     rationale -- string, >=80 chars explaining current shape + expected settling
#
# Bumping a ceiling requires editing this file in the SAME PR that grows the
# monitored file past the prior ceiling. There is no override label. The defense
# is `git blame` -- every intentional bump leaves a reviewable record with rationale.

# plan_backlinks.py was decomposed into a package (plan_backlinks/) in 2026-05-19.
# Monitoring the largest sub-file (core.py) post-split.
[files."src/backlink_publisher/cli/plan_backlinks/core.py"]
ceiling = 200
rationale = "D1 complexity refactor (2026-07-13): main() decomposed (CC 30→4) into _read_input_rows / _snapshot_gsc_baseline / _emit_success_nudges, and the argparse declaration moved to plan_backlinks/_cli_args.py (_build_parser). core.py dropped 244→164 SLOC — now set_log_level + input read + load_config + config_echo banner + reset_stats (H2) + plan_rows call + error-print + write_jsonl + preflight/canary nudge shell only. Ceiling 200 = round_up_to_10(164+30) per policy (ratcheted down from 250)."

[files."src/backlink_publisher/cli/publish_backlinks/__init__.py"]
ceiling = 290
rationale = "2026-06-05 feat/continuous-optimization: optimization integration added weight dispatch, queue telemetry, optimization-state hook. Measured SLOC=230. Ceiling 240 = round_up_to_10(230+10) per ratchet policy. (re-seeded 2026-07-06 post-reconcile-merge: SLOC=255, ceiling 290 = round_up_to_10(255+30))"

[files."src/backlink_publisher/content/fetch.py"]
ceiling = 250
rationale = "Added 5 env-var getter functions (BACKLINK_FETCH_*) for network constants. Measured SLOC grew to ~219 before this PR; adding ~25 SLOC for getters brings total to ~244. Ceiling raised 240->250 per ratchet policy."

[files."src/backlink_publisher/config/writer.py"]
ceiling = 240
rationale = "Wave 1 dofollow channels (Plan 2026-06-01-007): the GitLab Pages [gitlabpages] config section added _emit_gitlabpages_section (4 routing fields), a gitlabpages_config= save_config param, and its round-trip resolution — mirroring the [ghpages] wiring. SLOC rose 169→208. Ceiling 240 = round_up_to_10(208+30) per policy. Prior: Unit 2 monolith split (2026-05-20) extracted TOML/IO/merge helpers leaving only save_config()."

[files."src/backlink_publisher/_util/markdown.py"]
ceiling = 240
rationale = "Post-split: body templates (9 functions, ~130 SLOC) extracted to _body_templates.py in 2026-05-19. markdown.py now holds render+link+zh_short utilities at 210 SLOC; ceiling 240 provides 30-SLOC headroom for minor accretion."

[files."src/backlink_publisher/events/projector.py"]
ceiling = 110
rationale = "3 reducers (_project_checkpoint, _project_history, _project_drafts) extracted to events/_project_reducers.py (2026-05-27). projector.py dropped from 542 to 75 SLOC — holds only flush_for dispatch, record_projection_health, project_run_safe, and health constants. Ceiling 110 = round_up_to_10(75+30) per policy."

[files."src/backlink_publisher/events/_project_reducers.py"]
ceiling = 500
rationale = "Holds the 3 reducers (_project_checkpoint, _project_history, _project_drafts). Emit helpers (_emit_confirmed_history_row, _emit_drafts_confirmed, _parse_row_timestamps) extracted to _project_emit.py (2026-06-11). Ceiling tightened 620→500 = round_up_to_10(467+30) per policy."

[files."src/backlink_publisher/cli/publish_backlinks/_engine.py"]
ceiling = 480
rationale = "Publish dispatch engine: payload building, platform routing, checkpoint ops, auth expiry handling. Split from __init__.py during publish-backlinks package refactor. SLOC 448. Ceiling 480 = round_up_to_10(448+30) per policy."

[files."src/backlink_publisher/cli/admin/phase0_seal.py"]
ceiling = 460
rationale = "Phase 0 seal implementation: fill/deplete operations, seal validation, LLM body gen, sealing state machine. Full implementation (cli/phase0_seal.py is a 3-line shim re-exporting from here). SLOC 430. Ceiling 460 = round_up_to_10(430+30) per policy."

[files."src/backlink_publisher/cli/_resume.py"]
ceiling = 450
rationale = "Resume publish pipeline: checkpoint discovery, partial-run continuation, dedup gate, per-platform resume state. SLOC 423. Ceiling 450 = round_up_to_10(423+30) per policy."

[files."src/backlink_publisher/publishing/browser_publish/_chrome_session_impl.py"]
ceiling = 400
rationale = "Chrome CDP session implementation: launch/attach, cookie injection, navigation, screenshot, crash recovery. Core of browser-based publish flow. SLOC 374. Ceiling 400 = round_up_to_10(374+30) per policy."

[files."src/backlink_publisher/optimization/rules.py"]
ceiling = 400
rationale = "Dispatch weight optimization rules engine: signal collection, aggregated stats, floor clamping, weight computation. SLOC 371. Ceiling 400 = round_up_to_10(371+30) per policy."

[files."src/backlink_publisher/cli/plan/generate_backlink_text.py"]
ceiling = 400
rationale = "Backlink text generation: LLM prompt construction, multi-template body gen, link insertion, length enforcement. Full implementation (cli/generate_backlink_text.py is a 3-line shim). SLOC 366. Ceiling 400 = round_up_to_10(366+30) per policy."

[files."src/backlink_publisher/cli/_report_format.py"]
ceiling = 40
rationale = "Anchor report formatting: tabular output, platform breakdown, dofollow status, summary metrics. SLOC 363. Ceiling 390 = round_up_to_10(363+30) per policy. (re-seeded 2026-07-06 post-reconcile-merge: file is now a 4-SLOC back-compat shim to cli/publish/_report_format.py, ceiling 40 = round_up_to_10(4+30))"

[files."src/backlink_publisher/publishing/session/provider.py"]
ceiling = 380
rationale = "Browser session provider: storage state management, cookie refresh, session pool, concurrent access guard. SLOC 352. Ceiling 380 = round_up_to_10(352+30) per policy."

[files."src/backlink_publisher/cli/_dedup_ops.py"]
ceiling = 380
rationale = "Dedup operations: URL normalization, content hash comparison, duplicate detection across platform runs. SLOC 350. Ceiling 380 = round_up_to_10(350+30) per policy."

[files."webui_app/api/v1/schemas.py"]
ceiling = 640
rationale = "OpenAPI request/response schemas: typed payload models for pipeline, settings, channel bind, OAuth, LLM config endpoints. Pure declarative schemas aligned with spec.py. SLOC 474. Ceiling 500 = round_up_to_10(474+30) per policy. Plan 2026-07-02-001 U6 (2026-07-06): health-dashboard schemas +52 SLOC (merged 526). Plan 2026-07-06-004 Unit 2: MonitorCardItemSchema +29 SLOC; U5/W4 merge: HistoryItemSchema deleted_at + deleted/skipped fields +25 SLOC. Combined 582. Ceiling 620 re-baselined 2026-07-09: concurrent-branch endpoint merges (incl. onboarding) grew the file to 643 SLOC, exceeding 620; ceiling 680 = round_up_to_10(643+30) per ratchet policy, headroom 37. Re-tightened 2026-07-13 after the concurrent branches actually merged: measured SLOC=603 (some pre-counted endpoint schemas landed smaller), ceiling 640 = round_up_to_10(603+30)."

[files."webui_app/routes/sites.py"]
ceiling = 410
rationale = "Sites management routes: site CRUD, URL category editor, bulk import/export. SLOC 376. Ceiling 410 = round_up_to_10(376+30) per policy."

[files."webui_app/api/sites_api.py"]
ceiling = 340
rationale = "Sites API layer: site CRUD endpoints, validation, category operations. SLOC 310. Ceiling 340 = round_up_to_10(310+30) per policy."

[files."webui_store/channel_status.py"]
ceiling = 350
rationale = "Channel status store: mark_bound/mark_expired reconciliation, binding status queries, _LazyStore singleton. SLOC 279. Ceiling 310 = round_up_to_10(279+30) per policy. (re-seeded 2026-07-06 post-reconcile-merge: SLOC=311, ceiling 350 = round_up_to_10(311+30))"

[files."src/backlink_publisher/publishing/adapters/__init__.py"]
ceiling = 400
rationale = "U4 lazy-init refactor (2026-07-03): PEP 562 __getattr__ + _lazy_init() guard + register_all_adapters() in function scope moved 24 adapter imports out of module level. SLOC reached 368. Ceiling 400 = round_up_to_10(368+30) per ratchet policy."

[files."src/backlink_publisher/cli/phase0_seal.py"]
ceiling = 40
rationale = "U8 refactoring (2026-06-26): phase0_seal.py became a 3-line backward-compat shim (import + re-export from cli/admin/phase0_seal). Actual implementation lives in cli/admin/phase0_seal.py. Ceiling 40 = round_up_to_10(2+30) per policy."

[files."src/backlink_publisher/cli/plan_check.py"]
ceiling = 40
rationale = "U8 refactoring (2026-06-26): plan_check.py became a 3-line backward-compat shim (import + re-export from cli/plan/plan_check). Actual implementation lives in cli/plan/plan_check.py. Ceiling 40 = round_up_to_10(2+30) per policy."

[files."src/backlink_publisher/cli/validate_backlinks.py"]
ceiling = 150
rationale = "thin-WebUI Phase 2 Unit 6 (2026-05-27): the pure validation kernel (URL-collection + check_urls_strict + the per-row loop) extracted to validate/engine.py so the in-process PipelineAPI.validate path shares it. validate_backlinks.py dropped from 168 to 116 SLOC — now argparse + set_log_level + load_config_tolerant + config_echo banner + read_jsonl + a single validate_rows call + recon/emit_envelope_and_exit shell only. Ceiling 150 = round_up_to_10(116+30)."

[files."src/backlink_publisher/cli/report_anchors.py"]
ceiling = 40
rationale = "U8 refactoring (2026-06-26): report_anchors.py became a 3-line backward-compat shim (import + re-export from cli/publish/report_anchors). Actual implementation lives in cli/publish/report_anchors.py. Ceiling 40 = round_up_to_10(2+30) per policy."

[files."src/backlink_publisher/cli/_publish_helpers.py"]
ceiling = 530
rationale = "refactor 2026-06-01: extracted the CLI layer (_build_parser argparse + _handle_checkpoint_ops + _handle_auth_expired) to cli/_publish_cli.py. The _do_sleep/_sleep_with_throttle and verify_published/_do_verify mock seams stay here (tests patch them at this module). _publish_helpers re-exports the 3 moved names. Plan 2026-06-22-001 U4-3 (2026-06-22): split _publish_epilogue into a pure verdict (PublishExitDecision + _decide_publish_exit, the single source of truth shared with PublishOutcome.terminal_exit_code) plus the impure stdout/SystemExit dispatch, and added the _check_token_drift(raises=False) in-process variant. SLOC 442->493 (+51 for the pure decision + dataclass + docstrings, net of the inline epilogue branches removed). Ceiling 530 = round_up_to_10(493+30)."

[files."src/backlink_publisher/cli/generate_backlink_text.py"]
ceiling = 40
rationale = "U8 refactoring (2026-06-26): generate_backlink_text.py became a 3-line backward-compat shim (import + re-export from cli/plan/generate_backlink_text). Actual implementation lives in cli/plan/generate_backlink_text.py. Ceiling 40 = round_up_to_10(2+30) per policy."

# Plan 2026-06-02-003: canary-seed + platform-discovery (dofollow hardening).
# Three new files co-landed in the same PR; budget entries required by C2.
[files."src/backlink_publisher/_util/http_probe.py"]
ceiling = 210
rationale = "HTTP probe core extracted from scripts/channel_probe.py (Plan 2026-06-02-003 Unit 1). Contains Hit/UrlResult dataclasses, USER_AGENTS constants, _validate_url_ssrf/_probe/_triage private functions, and public probe_url(). SLOC 174 reflects the full three-UA probe + triage decision matrix. Ceiling 210 = round_up_to_10(174+30) per policy."

[files."src/backlink_publisher/cli/canary_seed.py"]
ceiling = 40
rationale = "U8 refactoring (2026-06-26): canary_seed.py became a 3-line backward-compat shim (import + re-export from cli/spray/canary_seed). Actual implementation lives in cli/spray/canary_seed.py. Ceiling 40 = round_up_to_10(2+30) per policy."

[files."scripts/platform_discovery.py"]
ceiling = 340
rationale = "platform-discovery script (Plan 2026-06-02-003 Unit 3): SERP → HTTP probe → browser-UA rel scan → scored JSONL candidate report. SLOC 306 includes DuckDuckGo HTML fetch, regex anchor extraction, dofollow_rate scoring, SSRF guard (fail-closed Track B), --candidate-urls fallback path, and progress RECON. Ceiling 340 = round_up_to_10(306+30) per policy."

[files."src/backlink_publisher/cli/pipeline_orchestrator.py"]
ceiling = 410
rationale = "Pipeline orchestrator (Plan 2026-06-10-002 U2.1): CLI entry point dispatching equity-ledger → plan-gap → plan-backlinks → validate → publish pipeline steps via subprocess. Current SLOC 379 after full-automation-upgrade merged and tightened scope. Ceiling 410 = round_up_to_10(379+30) per policy."
# ─────────────────────────────────────────────────────────────────────────────
# Plan 2026-06-15-002 P1-3: hotspot audit of every 500+ raw-LOC source file.
# The pre-existing budget covered only 17 files. This block extends coverage to
# all remaining 500+ raw-LOC files (radon SLOC < raw LOC because these files
# carry heavy docstrings/comments). Audit verdict per file is encoded in the
# rationale: KEEP (large-but-stable, ceiling freezes current shape), or
# CANDIDATE (worth a future split — ceiling freezes now, split is a separate
# plan-doc). None of these are open hot paths today; the ceilings exist to
# force a deliberate budget bump (with rationale) the moment any grows.
# Ceiling policy: round_up_to_10(current_radon_SLOC + 30) per the repo ratchet.
# ─────────────────────────────────────────────────────────────────────────────

# KEEP — WebUI store. ACID idempotency dedup: connection lifecycle + SQL +
# migration + retry are one cohesive unit; splitting across files would just
# spread the transactional coupling.
[files."src/backlink_publisher/idempotency/store.py"]
ceiling = 510
rationale = "Audit KEEP (2026-06-15): idempotency dedup store. radon SLOC 478 across connection lifecycle, schema migration, retry, and the public acquire/release API is one cohesive transactional unit — splitting read/write paths would spread SQL + locking coupling across files. Ceiling 510 = round_up_to_10(478+30); bump only with a written split rationale."

[files."src/backlink_publisher/publishing/_manifests.py"]
ceiling = 520
rationale = "Audit KEEP (2026-06-15): per-channel manifest declarations. radon SLOC 489 is a pure data table (UiMeta/BindDescriptor/Policy instances, 0 logic fns). Size scales linearly with channel count — splitting by channel would just fragment the single source of truth that test_manifest_contract.py validates. Ceiling 520 = round_up_to_10(489+30)."

# KEEP — single-platform adapter. GraphQL writePost + cookie auth + image
# upload is one cohesive publish unit; size tracks Velog's API surface.
[files."src/backlink_publisher/publishing/adapters/velog_graphql.py"]
ceiling = 450
rationale = "Audit KEEP (2026-06-15): Velog GraphQL adapter. radon SLOC 413 (writePost, cookie auth, image upload, null-after-retry diagnostics). One-platform adapter whose size tracks Velog's API surface; splitting auth from publish would duplicate the cookie-lifecycle coupling already shared via session/. Ceiling 450 = round_up_to_10(413+30)."

# KEEP — single-platform adapter. telegra.ph API + image upload.
[files."src/backlink_publisher/publishing/adapters/telegraph_api.py"]
ceiling = 390
rationale = "Audit KEEP (2026-06-15): Telegraph API adapter. radon SLOC 352 covers the single API path + image upload + banner embedding. One-platform adapter; banner upload is dispatched generically by banner_dispatcher and only the small embed_banner lives here. Ceiling 390 = round_up_to_10(352+30)."

# KEEP — per-platform verify logic, already extracted from adapters/__init__.py.
# Splitting per-platform would lose the shared verify contract.
[files."src/backlink_publisher/publishing/_verify_adapters.py"]
ceiling = 60
rationale = "Wave 3 Unit 3 (2026-06-11): _verify_adapters.py split into _verify_setup.py + _verify_live_probes.py. Residual _verify_adapters.py is now a thin shim (SLOC 28) re-exporting the public surface for backwards compat. Ceiling 60 = round_up_to_10(28+30) per policy."

# KEEP — frozen config dataclasses. 30 classes, near-zero logic; size tracks
# the config schema (blogger/medium/oauth/targets/etc), which is the SSoT.
[files."src/backlink_publisher/config/types.py"]
ceiling = 260
rationale = "Audit KEEP (2026-06-15): config dataclasses + constants. radon SLOC 222 across 30 frozen dataclasses with near-zero logic — this IS the config schema SSoT (blogger/medium/oauth/targets/throttle). Splitting would just re-distribute declarations the loader + writer both walk end-to-end. Ceiling 260 = round_up_to_10(222+30)."

[files."src/backlink_publisher/publishing/registry.py"]
ceiling = 364
rationale = "Plan 2026-07-06-002 Unit B2: pre-emptive raise (92.4% of prior 340 ceiling; re-measured SLOC=314, unchanged) so this file does not hard-fail mid-way through the concurrently-active v0.6.0 upgrade plan (docs/plans/2026-07-02-001-opt-v060-uiux-pipeline-upgrade-plan.md), whose U10 per-platform-lane parallelization is documented as touching this file. Coordination check performed: no branch in this workspace (all local worktree branches, origin/*, gitlab/*) has a commit ahead of main touching registry.py, and v0.6.0's own U10 Files list does not name registry.py directly -- no live in-flight work found to conflict with, so this raise proceeds unilaterally per plan fallback guidance. Ceiling set to the maximum this PR can take without itself tripping tests/test_no_monolith_regrowth.py::test_policy_to_seed_drift's unconditional SEED_HEADROOM_MAX=50 anti-drift gate (ceiling-SLOC<=50, no rationale override exists): 314+50=364, materially larger than the standard round_up_to_10(SLOC+30)=350 ratchet would give, to buffer both this plan's remaining units and v0.6.0's anticipated growth in one shot rather than a series of tiny bumps."

[files."src/backlink_publisher/cli/spray_backlinks/core.py"]
ceiling = 160
rationale = "Wave 3 Unit 1 split completed 2026-07-06 per plan 2026-07-06-003 U6: core.py trimmed 517→129 SLOC. Argparse moved to _args.py, seed loop + per-seed orchestration to _run.py, checkpoint/resume helpers to _gates.py (deduplicated — core's copies deleted). core.py keeps the I/O shell (read seeds, epilogue, exit codes) and the three test seams (_spray_checkpoint_dir/_default_rewrite_fn/dispatch_burst, dereferenced late). main() CC 29→4. Ceiling 160 = round_up_to_10(129+30)."

[files."src/backlink_publisher/cli/spray_backlinks/_args.py"]
ceiling = 180
rationale = "Plan 2026-07-06-003 U6 (2026-07-06): new file extracted from core.py completing the Wave 3 Unit 1 split. Holds _build_parser (14 argparse flags — declarative, near-zero logic), _parse_platforms, and _validate_args (closed-set flag validation, CC 13). SLOC 148. Ceiling 180 = round_up_to_10(148+30) per policy."

[files."src/backlink_publisher/cli/spray_backlinks/_run.py"]
ceiling = 250
rationale = "Plan 2026-07-06-003 U6 (2026-07-06): new file extracted from core.py completing the Wave 3 Unit 1 split. Holds the per-seed run orchestration: _run_seed_loop (checkpointed loop + inter-seed delay, CC 14), _process_seed (expand→gate→draft→audit, CC 11), _run_burst (audit gate + jittered dispatch, CC 12), _emit_preview. LLM rewrite factory and dispatcher are injected callables so core.py stays the test seam. SLOC 214. Ceiling 250 = round_up_to_10(214+30) per policy."

[files."src/backlink_publisher/cli/spray_backlinks/_engine.py"]
ceiling = 110
rationale = "Plan 2026-06-24-002 U2: deleted dead _run_spray (never called by main()) and its duplicate private helpers (_emit_preview, _main_anchor_of etc); _engine.py reverts to pure kernel only (SprayCandidate, expand_seed, validate_platform_selection, gate_candidates). 333 SLOC → 79 SLOC. Ceiling 110 = round_up_to_10(79+30)."

[files."src/backlink_publisher/cli/spray_backlinks/_gates.py"]
ceiling = 150
rationale = "Wave 3 Unit 1 (2026-06-11): extracted from core.py — cross-seed governance closure (_make_cross_seed_checker) + checkpoint helpers (_spray_checkpoint_dir, _checkpoint_path, _generate_run_id, _list_checkpoints, _save_checkpoint, _domain_slug, _write_per_seed_file). Plan 2026-07-06-003 U6 (2026-07-06) completed the split: core's duplicate copies deleted, this module became the live implementation and gained _setup_resume + _handle_list_checkpoints; checkpoint dir is now an explicit cdir param (core keeps the patchable seam). SLOC 119. Ceiling 150 = round_up_to_10(119+30) per policy."

# Wave 3 Unit 2 (2026-06-11): events/history_query.py split into read-path + write-path (mutations).
[files."src/backlink_publisher/events/history_query.py"]
ceiling = 440
rationale = "Wave 3 Unit 2 (2026-06-11): write-path mutations (purge_failed_from_db, delete_from_db, bulk_delete_from_db, update_status_in_db, _STATUS_TO_KIND) extracted to _history_mutations.py. history_query.py now holds read-path only plus re-exports for backward compat. SLOC dropped from 465 to 356. Ceiling 390 = round_up_to_10(356+30) per policy. W4 soft-delete reintegration (2026-07-07, U5/W4 merge): list_history/get_history_item gained the include_deleted='window' branch, the _deleted_at_cutoff_iso helper, and re-exports for undelete_from_db/CLIENT_UNDO_WINDOW_SECONDS/PURGE_WINDOW_SECONDS. SLOC 356->405. Ceiling 440 = round_up_to_10(405+30) per policy."

[files."src/backlink_publisher/events/_history_mutations.py"]
ceiling = 260
rationale = "Wave 3 Unit 2 (2026-06-11): new file extracted from history_query.py. Contains all write-path mutations: purge_failed_from_db (finds failed article IDs + orphan events), delete_from_db (single item incl. evt- prefix), bulk_delete_from_db (batch delete articles+events), update_status_in_db (kind+ui_status mutation), and _STATUS_TO_KIND dict. SLOC 130. Ceiling 160 = round_up_to_10(130+30) per policy. W4 soft-delete reintegration (2026-07-07, U5/W4 merge): delete_from_db/bulk_delete_from_db now soft-delete (set deleted_at) instead of hard DELETE, plus new undelete_from_db, _purge_expired_rows, _cutoff_iso, and the CLIENT_UNDO_WINDOW_SECONDS/PURGE_WINDOW_SECONDS constants. SLOC 130->222. Ceiling 260 = round_up_to_10(222+30) per policy."

# Wave 3 Unit 5 (2026-06-11): webui_app/routes/pipeline.py split into plan/generate/validate + publish.
# webui_app/ is NOT auto-scanned by monolith_budget; entries here are explicit.
[files."webui_app/routes/pipeline_plan.py"]
ceiling = 290
rationale = "Wave 3 Unit 5 (2026-06-11): new file split from routes/pipeline.py. Contains ce_plan (URL input + multi-URL config + session setup), ce_generate (seed building + plan-backlinks call), ce_validate, ce_preview, and ce_regen_body (LLM body re-gen endpoint). All plan-side routes share _api=PipelineAPI() and the plan_logger. Tests that patch fetch_url_metadata or _api for these routes point to this module. SLOC 249. Ceiling 290 = round_up_to_10(249+30) per policy."

[files."webui_app/routes/pipeline_publish.py"]
ceiling = 220
rationale = "Wave 3 Unit 5 (2026-06-11): new file split from routes/pipeline.py. Contains ce_publish (single publish step with typed error surfacing) and ce_publish_chain (one-click plan→validate→publish). Both routes share _api=PipelineAPI() and call _push_history_* helpers. Tests that patch _api for publish routes point to this module. SLOC 179. Ceiling 220 = round_up_to_10(179+30) per policy."

# Wave 3 Unit 4 (2026-06-11): webui_app/services/keepalive_job.py split — engine helpers extracted.
# webui_app/ is NOT auto-scanned by monolith_budget; entries here are explicit.
[files."webui_app/services/keepalive_job.py"]
ceiling = 430
rationale = "Wave 3 Unit 4 (2026-06-11): engine helpers (_default_candidates, _default_unverified_candidates, _default_probe, 3 job dataclasses, _default_republish_gaps, _default_publish_seed, _default_persist, KeepAliveResult, per-site lock helpers, run_keepalive_for_site) extracted to _keepalive_engine.py. Plan 2026-06-22-001 U2 (2026-06-22): _ensure_article moved to core backlink_publisher.events._project_helpers (re-exported here for back-compat — tests call keepalive_job._ensure_article directly) to break the keepalive/chain.py→webui_app reverse import; _default_reverify and recheck_link stay here because tests patch keepalive_job.recheck_link and call them directly. KeepaliveJobRegistry + registry singleton remain as core concern. SLOC dropped 533→412 at the seed, since trimmed to 394. Ceiling 430 = round_up_to_10(394+30) per policy (ratcheted down from 450 to keep headroom <=50)."

[files."webui_app/services/_keepalive_engine.py"]
ceiling = 180
rationale = "Wave 3 Unit 4 (2026-06-11): new file extracted from keepalive_job.py. Contains constants (_RUNTIME_STICKY, _PROBE_ERROR, _DEAD_VERDICTS), default candidate/probe helpers, 3 job dataclasses (KeepaliveJob, GapClosureJob, RepublishJob), republish gap/publish/persist helpers, KeepAliveResult, per-site lock state, and run_keepalive_for_site. Tests that exercise run_keepalive_for_site patch _keepalive_engine.* names. SLOC 147. Ceiling 180 = round_up_to_10(147+30) per policy."

# Wave 3 Unit 3 (2026-06-11): publishing/_verify_adapters.py split into setup + live probes.
[files."src/backlink_publisher/publishing/_verify_setup.py"]
ceiling = 240
rationale = "Wave 3 Unit 3 (2026-06-11): new file split from _verify_adapters.py. Contains offline setup checks (_check_*_setup, _SETUP_CHECKS dict), adapter imports for 8 platforms, verify_adapter_setup (public entry + registry fallback), and _verify_dry_run. verify_adapter_setup calls _verify_live via lazy import (inside function) to avoid circular module-level dependency with _verify_live_probes.py. SLOC 202. Ceiling 240 = round_up_to_10(202+30) per policy."

[files."src/backlink_publisher/publishing/_verify_live_probes.py"]
ceiling = 290
rationale = "Wave 3 Unit 3 (2026-06-11): new file split from _verify_adapters.py. Contains UTC helper, 6 VerifyResult factory functions, _verify_live dispatcher, and 4 per-platform live probe implementations (telegraph, ghpages, blogger, velog) with their constants. Imports verify_adapter_setup at module level from _verify_setup (no circular since _verify_setup uses lazy import for _verify_live). SLOC 251. Ceiling 290 = round_up_to_10(251+30) per policy."

# Plan 2026-06-16-003: GSC indexation + ranking panels added to health dashboard.
# webui_app/ is NOT auto-scanned by monolith_budget; entries below are explicit.
[files."webui_app/routes/health.py"]
ceiling = 630
rationale = "Plan 2026-06-16-003 (2026-06-16): added _gsc_indexation_panel() and _gsc_ranking_panel() panel functions (+35 SLOC) plus two _g_cache calls in the main route. Also registers this file explicitly since webui_app/ is not auto-scanned. SLOC after change: 447. Ceiling 490 = round_up_to_10(447+30) per policy. (re-seeded 2026-07-06 post-reconcile-merge: SLOC=592, ceiling 630 = round_up_to_10(592+30))"

[files."webui_app/health_metrics.py"]
ceiling = 810
rationale = "Plan 2026-06-16-003 (2026-06-16): added indexation_status() and ranking_trend() query functions (+85 SLOC). Also registers this file explicitly since webui_app/ is not auto-scanned. SLOC after change: 330. Ceiling 370 = round_up_to_10(330+30) per policy. (re-seeded 2026-07-06 post-reconcile-merge: SLOC=774, ceiling 810 = round_up_to_10(774+30))"

[files."webui_app/api/v1/spec.py"]
ceiling = 1690
rationale = "OpenAPI 3.1 contract spec.py. Grows per migrated endpoint group. Plan 2026-07-02-001 U6 adds health-dashboard endpoints (+~70 SLOC to merged 1507). Ceiling 1650 = round_up_to_10(1507+70+30) per policy. Concurrent-branch merge: U6 + U5 pagination + U4 Unit 2 MonitorCardItemSchema + W4 soft-delete. Re-seeded 2026-07-07: actual SLOC=1652 after further concurrent merges, ceiling 1690. Re-baselined 2026-07-09: further concurrent-branch endpoint merges (incl. onboarding) pushed actual SLOC to 1753, exceeding 1690; ceiling 1790 = round_up_to_10(1753+30) per ratchet policy, headroom 37. Re-tightened 2026-07-13 after the concurrent branches actually merged: measured SLOC=1652, ceiling 1690 = round_up_to_10(1652+30)."

# ─────────────────────────────────────────────────────────────────────────────
# Plan 2026-07-06-002 Unit B1: hidden-debt hardening sweep — SLOC budget
# completeness pass. These 11 files sit in the 300-370 SLOC range and were
# never given a budget entry, so they were previously unmonitored (not
# over-budget). Each is newly-tracked here, not newly-over-budget: no file
# grew past a prior ceiling, they simply lacked one. Ceiling policy unchanged:
# round_up_to_10(current_radon_SLOC + 30) per the repo ratchet.
# ─────────────────────────────────────────────────────────────────────────────

[files."src/backlink_publisher/publishing/adapters/medium_brave.py"]
ceiling = 400
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: macOS Brave-via-AppleScript Medium adapter (stable window/tab-id resolution, clipboard paste into the editor, keyboard-shortcut publish sequence). SLOC 366. Ceiling 400 = round_up_to_10(366+30) per policy."

[files."src/backlink_publisher/keepalive/chain.py"]
ceiling = 380
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: five-step keep-alive orchestrator (recheck -> status derivation -> gap planning -> publish -> reverify+stat feedback) with fully injectable dependencies for network-free testing. SLOC 346. Ceiling 380 = round_up_to_10(346+30) per policy."

[files."src/backlink_publisher/cli/ops/probe_citations.py"]
ceiling = 380
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: GEO AI-citation probe CLI verb wiring selection -> guard -> probe -> classify -> emit -> cursor-advance, with a dry-run-by-default cost-ceiling preview. SLOC 341. Ceiling 380 = round_up_to_10(341+30) per policy."

[files."src/backlink_publisher/publishing/adapters/ghpages.py"]
ceiling = 360
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: GitHub Pages adapter using the Contents API (no git subprocess, Bearer PAT auth) — the highest-SEO-value Phase 3 dofollow channel. SLOC 329. Ceiling 360 = round_up_to_10(329+30) per policy."

[files."src/backlink_publisher/cli/_bind/chrome_backend.py"]
ceiling = 360
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: real Chrome/CDP bind-channel backend that launches a visible profile for manual operator OAuth/2FA and exports host-filtered cookies into the shared storage_state shape. SLOC 323. Ceiling 360 = round_up_to_10(323+30) per policy."

[files."src/backlink_publisher/recheck/selection.py"]
ceiling = 350
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: age-based recheck-backlinks candidate selection sourced from the append-only publish.confirmed event stream, decoupled from the history_store->events.db migration. SLOC 315. Ceiling 350 = round_up_to_10(315+30) per policy."

[files."src/backlink_publisher/cli/_seal_init.py"]
ceiling = 350
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: phase0-seal init subcommand extracted from phase0_seal.py to keep the parent CLI module within its own SLOC ceiling. SLOC 315. Ceiling 350 = round_up_to_10(315+30) per policy."

[files."src/backlink_publisher/publishing/reliability/circuit.py"]
ceiling = 350
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: per-channel circuit breaker with half-open recovery testing, multi-error-type trip conditions, and a fail-CLOSED flock-protected JSON state file. SLOC 312. Ceiling 350 = round_up_to_10(312+30) per policy."

[files."src/backlink_publisher/events/store.py"]
ceiling = 340
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: SQLite-backed EventStore for the read-side events.db projection (sibling of JsonStore), with WAL journal mode and rebuildable-from-JSON durability tradeoffs. SLOC 308. Ceiling 340 = round_up_to_10(308+30) per policy."

[files."src/backlink_publisher/cli/_bind/_driver_impl.py"]
ceiling = 340
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: Playwright headed driver for bind-channel — storage_state path validation, lazy Playwright import, login-URL navigation, and bound_predicate polling. SLOC 306. Ceiling 340 = round_up_to_10(306+30) per policy."

[files."src/backlink_publisher/publishing/adapters/medium_browser.py"]
ceiling = 340
rationale = "Newly-tracked during 2026-07-06 hardening audit (Unit B1), not newly-over-budget: Medium browser fallback adapter using Playwright with a cookies-only auth hard-cut (medium-cookies.json), replacing the retired storage_state contract. SLOC 304. Ceiling 340 = round_up_to_10(304+30) per policy."

# Plan 2026-07-09-001 (onboarding wizard): explicit entries — webui_app/ and
# webui_store/ are NOT auto-scanned by monolith_budget, so new files are opt-in.
[files."webui_store/onboarding.py"]
ceiling = 40
rationale = "Plan 2026-07-09-001 (onboarding wizard): single-row BlobSqliteStore holding only the operator's 'dismissed' flag for the first-run guide. Step completion is derived live in OnboardingAPI from real system state, so this store carries no driftable checklist. Measured SLOC=10. Ceiling 40 = round_up_to_10(10+30) per ratchet policy."

[files."webui_app/api/onboarding_api.py"]
ceiling = 110
rationale = "Plan 2026-07-09-001 (onboarding wizard): transport-neutral OnboardingAPI facade aggregating the 5-step first-run happy path, deriving each step's done flag live from ChannelOverviewAPI / app_meta / GlobalSettingsAPI / campaign_store / history_store. No step state is persisted here. Measured SLOC=83. Ceiling 110 = round_up_to_10(83+30) per ratchet policy."

# Plan 2026-07-09-001 (error/bug-report system): capture any failure into a
# self-contained, secret-redacted bundle for handing to a coding agent.
[files."src/backlink_publisher/cli/report_bug/_build.py"]
ceiling = 520
rationale = "Plan 2026-07-09-001 (error/bug-report system): core assembly of the coding-agent-ready diagnostic bundle — environment/config/health/recent-runs snapshots, typed-error section, free-text + structured redaction, error-class self-diagnosis, and Markdown/JSON rendering. Reused by both the bp-report-bug CLI and the WebUI export-bundle endpoint. Measured SLOC=349. Ceiling 380 = round_up_to_10(349+30) per ratchet policy. Re-measured 2026-07-13 at merge time: the branch's ce-code-review fix round (P1 security/correctness) plus render_markdown remediation-hint expansion grew the file to SLOC=486 before it ever landed; ceiling 520 = round_up_to_10(486+30)."

[files."src/backlink_publisher/cli/report_bug/main.py"]
ceiling = 260
rationale = "Plan 2026-07-09-001 (error/bug-report system): bp-report-bug CLI entrypoint — --wrap subprocess capture (stdout passthrough, stderr buffered), --stderr-file / stdin capture, interactive describe prompt, and report write (0600) with JSONL stdout plus human stderr summary. Measured SLOC=134. Ceiling 170 = round_up_to_10(134+30) per ratchet policy. Re-measured 2026-07-13 at merge time: --wrap run-id extraction, exit-code passthrough and the code-review fix round grew the entrypoint to SLOC=223 before it ever landed; ceiling 260 = round_up_to_10(223+30)."

[files."webui_app/api/v1/error_report_bundle.py"]
ceiling = 120
rationale = "Plan 2026-07-09-001 (error/bug-report system): POST /api/v1/error-reports/export-bundle — WebUI counterpart to bp-report-bug; reuses Unit-1 sanitizer (known-credential matching) then the shared core builder to enrich a failed run's context and return a redacted Markdown bundle. Measured SLOC=82. Ceiling 120 = round_up_to_10(82+30) per ratchet policy."
