# Monolith SLOC budget. See:
# - docs/plans/2026-05-18-006-feat-monolith-sloc-ceiling-plan.md
# - docs/brainstorms/2026-05-18-monolith-loc-ceiling-requirements.md
#
# Schema (enforced by tests/test_no_monolith_regrowth.py):
#   each [files."<path>"] entry MUST have:
#     ceiling  -- int, radon SLOC ceiling
#     rationale -- string, >=80 chars explaining current shape + expected settling
#
# Bumping a ceiling requires editing this file in the SAME PR that grows the
# monitored file past the prior ceiling. There is no override label. The defense
# is `git blame` -- every intentional bump leaves a reviewable record with rationale.

# plan_backlinks.py was decomposed into a package (plan_backlinks/) in 2026-05-19.
# Monitoring the largest sub-file (core.py) post-split.
[files."src/backlink_publisher/cli/plan_backlinks/core.py"]
ceiling = 250
rationale = "thin-WebUI Phase 2 Unit 7 (2026-05-28): pure generation kernel (_cell_gate_drop, _dispatch_row, _emit_link_count_recon, _scheduler_enabled_for, PlanOutcome, plan_rows) extracted to plan_backlinks/_engine.py so the in-process PipelineAPI.plan path shares it. core.py dropped from 392 to 208 SLOC — now argparse + set_log_level + row parsing + load_config + config_echo banner + reset_stats (H2) + plan_rows call + error-print + write_jsonl + preflight/canary nudge shell only. Ceiling 250 = round_up_to_10(208+30) per policy."

[files."src/backlink_publisher/cli/publish_backlinks/__init__.py"]
ceiling = 240
rationale = "2026-06-05 feat/continuous-optimization: optimization integration added ~50 SLOC (weight dispatch, queue telemetry, optimization-state hook) on top of the 2026-06-02 loop-extraction refactor (SLOC 159). Current SLOC=209. Ceiling 240 = round_up_to_10(209+30) per ratchet policy."

[files."src/backlink_publisher/content/fetch.py"]
ceiling = 250
rationale = "Added 5 env-var getter functions (BACKLINK_FETCH_*) for network constants. Measured SLOC grew to ~219 before this PR; adding ~25 SLOC for getters brings total to ~244. Ceiling raised 240->250 per ratchet policy."

[files."src/backlink_publisher/config/writer.py"]
ceiling = 240
rationale = "Wave 1 dofollow channels (Plan 2026-06-01-007): the GitLab Pages [gitlabpages] config section added _emit_gitlabpages_section (4 routing fields), a gitlabpages_config= save_config param, and its round-trip resolution — mirroring the [ghpages] wiring. SLOC rose 169→208. Ceiling 240 = round_up_to_10(208+30) per policy. Prior: Unit 2 monolith split (2026-05-20) extracted TOML/IO/merge helpers leaving only save_config()."

[files."src/backlink_publisher/_util/markdown.py"]
ceiling = 240
rationale = "Post-split: body templates (9 functions, ~130 SLOC) extracted to _body_templates.py in 2026-05-19. markdown.py now holds render+link+zh_short utilities at 210 SLOC; ceiling 240 provides 30-SLOC headroom for minor accretion."

[files."src/backlink_publisher/events/projector.py"]
ceiling = 110
rationale = "3 reducers (_project_checkpoint, _project_history, _project_drafts) extracted to events/_project_reducers.py (2026-05-27). projector.py dropped from 542 to 75 SLOC — holds only flush_for dispatch, record_projection_health, project_run_safe, and health constants. Ceiling 110 = round_up_to_10(75+30) per policy."

[files."src/backlink_publisher/events/_project_reducers.py"]
ceiling = 620
rationale = "Holds the 3 extracted reducers (_project_checkpoint, _project_history, _project_drafts) plus the confirmed-emit helpers _emit_confirmed_history_row (#350) and _emit_drafts_confirmed (#340, extracted 2026-06-01 to bring _project_drafts CC under the CC-30 backstop). _project_checkpoint decomposed into 4 named handlers (2026-06-03, CC 39→24). At 583 SLOC; ceiling 620 = round_up_to_10(583+30) per policy."

[files."src/backlink_publisher/publishing/adapters/__init__.py"]
ceiling = 340
rationale = "U3 auto-scan catalog registration (Plan 2026-06-05): register_catalog_entries() function (import-time YAML scan + _CATALOG_AUTO_REGISTERED cache) + tests added ~46 SLOC to reach 302. Ceiling=round_up_to_10(302+30)=340 per ratchet policy."

[files."src/backlink_publisher/cli/phase0_seal.py"]
ceiling = 465
rationale = "_handle_init (173 lines) + its exclusive helpers (_build_routine_verdict_ref, _build_manual_verdict_ref, _get_main_sha, _post_push_verify, _InitError, _NotesPushDidNotLand) extracted to _seal_init.py (2026-05-22). phase0_seal.py dropped from 720 to 449 SLOC — 7 subcommand dispatch layer + _handle_{show,verify,reseal,verify_hook} + shared helpers remain. Ceiling 465 = round_up_to_10(435+30)."

[files."src/backlink_publisher/cli/plan_check.py"]
ceiling = 260
rationale = "Three-tier decomposition (2026-05-26): schema (frontmatter/claims/filename-date-lock) moved to _plan_check_schema.py (166 SLOC), git resolution (fetch freshness, path/sha checks, stderr classification) to _plan_check_git.py (148 SLOC), output formatting (JSON/human/RECON lines) to _plan_check_format.py (76 SLOC). plan_check.py now holds main() + re-exports only at 227 SLOC. Ceiling 260 = round_up_to_10(227+30) per policy."

[files."src/backlink_publisher/cli/validate_backlinks.py"]
ceiling = 150
rationale = "thin-WebUI Phase 2 Unit 6 (2026-05-27): the pure validation kernel (URL-collection + check_urls_strict + the per-row loop) extracted to validate/engine.py so the in-process PipelineAPI.validate path shares it. validate_backlinks.py dropped from 168 to 116 SLOC — now argparse + set_log_level + load_config_tolerant + config_echo banner + read_jsonl + a single validate_rows call + recon/emit_envelope_and_exit shell only. Ceiling 150 = round_up_to_10(116+30)."

[files."src/backlink_publisher/cli/report_anchors.py"]
ceiling = 120
rationale = "thin-WebUI Phase 2 Unit 7 (2026-05-28): main() split into two engine calls (report_from_profile + report_from_rows) extracted to _report_engine.py so the in-process PipelineAPI.report_anchors path shares it. report_anchors.py stayed at 89 SLOC — holds argparse + load_config + config_echo banner + engine call + stdout print + stderr breach lines + emit_envelope_and_exit shell only. Ceiling 120 = round_up_to_10(89+30) per policy."

[files."src/backlink_publisher/cli/_publish_helpers.py"]
ceiling = 480
rationale = "refactor 2026-06-01: extracted the CLI layer (_build_parser argparse + _handle_checkpoint_ops + _handle_auth_expired) to cli/_publish_cli.py. The _do_sleep/_sleep_with_throttle and verify_published/_do_verify mock seams stay here (tests patch them at this module). _publish_helpers re-exports the 3 moved names. SLOC 634->442. Ceiling 480 = round_up_to_10(442+30)."

[files."src/backlink_publisher/cli/generate_backlink_text.py"]
ceiling = 390
rationale = "refactor 2026-06-01: extracted the input-parsing + deterministic-validation cluster (9 funcs + their constants) to cli/_candidates.py; this module now holds CLI/argparse + LLM orchestration (main/_resolve_client/_run_dry_run/_run_generate) and re-exports the moved names for the tests that import them. SLOC 541->357. Ceiling 390 = round_up_to_10(357+30)."

# Plan 2026-06-02-003: canary-seed + platform-discovery (dofollow hardening).
# Three new files co-landed in the same PR; budget entries required by C2.
[files."src/backlink_publisher/_util/http_probe.py"]
ceiling = 210
rationale = "HTTP probe core extracted from scripts/channel_probe.py (Plan 2026-06-02-003 Unit 1). Contains Hit/UrlResult dataclasses, USER_AGENTS constants, _validate_url_ssrf/_probe/_triage private functions, and public probe_url(). SLOC 174 reflects the full three-UA probe + triage decision matrix. Ceiling 210 = round_up_to_10(174+30) per policy."

[files."src/backlink_publisher/cli/canary_seed.py"]
ceiling = 230
rationale = "canary-seed CLI (Plan 2026-06-02-003 Unit 2): publishes a minimal test post to a dofollow=uncertain platform, inspects the target anchor rel, and emits advisory verdict JSONL. SLOC 196 includes argparse, cohort gate, credential check, stub payload builder, publish dispatch, SSRF guard, inspect_target_anchor call, and JSONL+RECON emit. Ceiling 230 = round_up_to_10(196+30) per policy."

[files."scripts/platform_discovery.py"]
ceiling = 340
rationale = "platform-discovery script (Plan 2026-06-02-003 Unit 3): SERP → HTTP probe → browser-UA rel scan → scored JSONL candidate report. SLOC 306 includes DuckDuckGo HTML fetch, regex anchor extraction, dofollow_rate scoring, SSRF guard (fail-closed Track B), --candidate-urls fallback path, and progress RECON. Ceiling 340 = round_up_to_10(306+30) per policy."

# ─────────────────────────────────────────────────────────────────────────────
# Plan 2026-06-15-002 P1-3: hotspot audit of every 500+ raw-LOC source file.
# The pre-existing budget covered only 17 files. This block extends coverage to
# all remaining 500+ raw-LOC files (radon SLOC < raw LOC because these files
# carry heavy docstrings/comments). Audit verdict per file is encoded in the
# rationale: KEEP (large-but-stable, ceiling freezes current shape), or
# CANDIDATE (worth a future split — ceiling freezes now, split is a separate
# plan-doc). None of these are open hot paths today; the ceilings exist to
# force a deliberate budget bump (with rationale) the moment any grows.
# Ceiling policy: round_up_to_10(current_radon_SLOC + 30) per the repo ratchet.
# ─────────────────────────────────────────────────────────────────────────────

# KEEP — WebUI store. ACID idempotency dedup: connection lifecycle + SQL +
# migration + retry are one cohesive unit; splitting across files would just
# spread the transactional coupling.
[files."src/backlink_publisher/idempotency/store.py"]
ceiling = 510
rationale = "Audit KEEP (2026-06-15): idempotency dedup store. radon SLOC 478 across connection lifecycle, schema migration, retry, and the public acquire/release API is one cohesive transactional unit — splitting read/write paths would spread SQL + locking coupling across files. Ceiling 510 = round_up_to_10(478+30); bump only with a written split rationale."

# CANDIDATE — WebUI async job registry. 9 classes; the recheck cycle, thread
# pool, and per-job state could separate. The U7 autopilot extraction already
# pulled out run_keepalive_for_site; further split is a tracked candidate.
[files."webui_app/services/keepalive_job.py"]
ceiling = 570
rationale = "Audit CANDIDATE (2026-06-15): keep-alive async job registry. radon SLOC 533 across 9 classes (registry, recheck cycle, thread pool, per-job state). U7 already extracted run_keepalive_for_site to a synchronous primitive; further split of the registry from the recheck cycle is a tracked candidate. Ceiling 570 = round_up_to_10(533+30) freezes current size; any growth must bump with split rationale."

# KEEP — pure per-channel manifest declarations (UiMeta/BindDescriptor/Policy).
# Zero classes/fns: it's a data table, not logic. Size tracks channel count.
[files."src/backlink_publisher/publishing/_manifests.py"]
ceiling = 520
rationale = "Audit KEEP (2026-06-15): per-channel manifest declarations. radon SLOC 489 is a pure data table (UiMeta/BindDescriptor/Policy instances, 0 logic fns). Size scales linearly with channel count — splitting by channel would just fragment the single source of truth that test_manifest_contract.py validates. Ceiling 520 = round_up_to_10(489+30)."

# KEEP — single-platform adapter. GraphQL writePost + cookie auth + image
# upload is one cohesive publish unit; size tracks Velog's API surface.
[files."src/backlink_publisher/publishing/adapters/velog_graphql.py"]
ceiling = 450
rationale = "Audit KEEP (2026-06-15): Velog GraphQL adapter. radon SLOC 413 (writePost, cookie auth, image upload, null-after-retry diagnostics). One-platform adapter whose size tracks Velog's API surface; splitting auth from publish would duplicate the cookie-lifecycle coupling already shared via session/. Ceiling 450 = round_up_to_10(413+30)."

# KEEP — single-platform adapter. telegra.ph API + image upload.
[files."src/backlink_publisher/publishing/adapters/telegraph_api.py"]
ceiling = 390
rationale = "Audit KEEP (2026-06-15): Telegraph API adapter. radon SLOC 352 covers the single API path + image upload + banner embedding. One-platform adapter; banner upload is dispatched generically by banner_dispatcher and only the small embed_banner lives here. Ceiling 390 = round_up_to_10(352+30)."

# KEEP — per-platform verify logic, already extracted from adapters/__init__.py.
# Splitting per-platform would lose the shared verify contract.
[files."src/backlink_publisher/publishing/_verify_adapters.py"]
ceiling = 480
rationale = "Audit KEEP (2026-06-15): per-platform verify logic extracted from adapters/__init__.py. radon SLOC 446 holds the shared post-publish verify contract across platforms; splitting per-platform would fragment the uniform verify_published() dispatch site that publish_backlinks calls. Ceiling 480 = round_up_to_10(446+30)."

# KEEP — frozen config dataclasses. 30 classes, near-zero logic; size tracks
# the config schema (blogger/medium/oauth/targets/etc), which is the SSoT.
[files."src/backlink_publisher/config/types.py"]
ceiling = 260
rationale = "Audit KEEP (2026-06-15): config dataclasses + constants. radon SLOC 222 across 30 frozen dataclasses with near-zero logic — this IS the config schema SSoT (blogger/medium/oauth/targets/throttle). Splitting would just re-distribute declarations the loader + writer both walk end-to-end. Ceiling 260 = round_up_to_10(222+30)."

# KEEP — events.db read query layer. 16 pure query fns; splitting by event
# kind would duplicate the SQL connection + parameter binding helpers.
[files."src/backlink_publisher/events/history_query.py"]
ceiling = 500
rationale = "Audit KEEP (2026-06-15): events.db read query layer. radon SLOC 465 across 16 pure query fns sharing one connection + parameter-binding pattern. Splitting by event kind (checkpoint/history/drafts) would duplicate the SQL plumbing; the write side already lives in store.py. Ceiling 500 = round_up_to_10(465+30)."

# KEEP — Publisher ABC + table dispatcher. The dofollow/manifest/visibility
# wiring is the registry contract; splitting would fragment the register() API.
[files."src/backlink_publisher/publishing/registry.py"]
ceiling = 300
rationale = "Audit KEEP (2026-06-15): Publisher ABC + table-driven dispatcher. radon SLOC 270 holds the register() contract (dofollow/manifest/visibility/rejection-map) and dispatch(); the adapter __init__.py and manifest_contract.py both depend on this surface as one unit. Ceiling 300 = round_up_to_10(270+30)."

# KEEP — spray-backlinks CLI shell. Already decomposed; holds argparse + the
# multi-seed orchestration loop that spray_backlinks/__init__.py delegates to.
[files."src/backlink_publisher/cli/spray_backlinks/core.py"]
ceiling = 530
rationale = "Audit KEEP (2026-06-15): spray-backlinks CLI shell. radon SLOC 494 is argparse + the multi-seed orchestration loop (seed-delay/max-seeds) delegating to plan/publish. Already a package (spray_backlinks/); core.py is the thin shell the package __init__ calls. Ceiling 530 = round_up_to_10(494+30)."

# KEEP — Chrome/CDP session helpers. 4 classes share the CDP attach lifecycle;
# splitting would spread the WebSocket plumbing that ChromeAttachSession owns.
[files."src/backlink_publisher/publishing/browser_publish/_chrome_session_impl.py"]
ceiling = 410
rationale = "Audit KEEP (2026-06-15): Chrome/CDP session helpers. radon SLOC 375 across 4 classes (ChromeAttachSession ctx mgr + helpers) sharing the CDP attach + WebSocket lifecycle. Splitting would spread the connection plumbing the context manager owns. Ceiling 410 = round_up_to_10(375+30)."

# KEEP — resume-from-checkpoint. The checkpoint replay + lease restoration is
# one cohesive recovery unit; splitting read-replay from write-restore would
# duplicate the checkpoint-row schema both sides assume.
[files."src/backlink_publisher/cli/_resume.py"]
ceiling = 460
rationale = "Audit KEEP (2026-06-15): resume interrupted publish run. radon SLOC 421 holds checkpoint replay + lease restoration as one recovery unit; the read-replay and write-restore halves share the checkpoint-row schema, so splitting would duplicate that contract. Ceiling 460 = round_up_to_10(421+30)."

# KEEP — single-platform adapter (ghpages Contents API + banner embed).
[files."src/backlink_publisher/publishing/adapters/ghpages.py"]
ceiling = 360
rationale = "Audit KEEP (2026-06-15): GitHub Pages adapter. radon SLOC 327 covers Contents API publish + banner embed (commit to assets/banners/). One-platform adapter; banner upload dispatched generically, only embed_banner lives here. Ceiling 360 = round_up_to_10(327+30)."

# CANDIDATE — Playwright bind driver. 9 classes; the headed-session lifecycle,
# storage-state write, and channel-recipe dispatch could separate. Tracked.
[files."src/backlink_publisher/cli/_bind/_driver_impl.py"]
ceiling = 340
rationale = "Audit CANDIDATE (2026-06-15): Playwright headed bind driver. radon SLOC 305 across 9 classes (session lifecycle, storage-state write, recipe dispatch). The recipe/<channel>.py files already absorb per-channel specifics; further split of session-lifecycle from storage-write is a tracked candidate. Ceiling 340 = round_up_to_10(305+30)."

# CANDIDATE — /ce:health dashboard. 7 read-only subroutes (scorecard links,
# publish-metrics, storage health, platform health) could split into a package
# mirroring the routes/ convention. Tracked; currently stable.
[files."webui_app/routes/health.py"]
ceiling = 400
rationale = "Audit CANDIDATE (2026-06-15): /ce:health publishing-health dashboard. radon SLOC 367 across 7 read-only subroutes + the storage/platform health helpers. Could split into a health/ blueprint package mirroring routes/ convention; currently stable, tracked candidate. Ceiling 400 = round_up_to_10(367+30)."

# KEEP — Medium bind recipe. The headed-login + storage-state + writePost
# validation is one channel-specific lifecycle; size tracks Medium's auth flow.
[files."src/backlink_publisher/cli/_bind/recipes/medium.py"]
ceiling = 290
rationale = "Audit KEEP (2026-06-15): Medium binding recipe. radon SLOC 259 is one channel-specific lifecycle (headed login + storage-state + post-validation). Per-channel recipes are intentionally separate files; this one's size tracks Medium's auth flow, not generic complexity. Ceiling 290 = round_up_to_10(259+30)."

# KEEP — events.db EventStore (write side). WAL config + append + retry + the
# JsonStore→EventStore projection are one cohesive persistence unit.
[files."src/backlink_publisher/events/store.py"]
ceiling = 340
rationale = "Audit KEEP (2026-06-15): events.db EventStore (write side). radon SLOC 306 holds WAL config + append + retry + projection plumbing; the SQLite helpers are already extracted to _store_sqlite.py and read queries to history_query.py. Remaining size is the persistence contract as one unit. Ceiling 340 = round_up_to_10(306+30)."

# KEEP — post-publish link-attr verifier. The dofollow/rel/survival checks
# share the fetch + parse pipeline; splitting per-attribute would duplicate it.
[files."src/backlink_publisher/publishing/adapters/link_attr_verifier.py"]
ceiling = 320
rationale = "Audit KEEP (2026-06-15): post-publish link-attribute verifier. radon SLOC 285 runs the shared fetch + parse pipeline for dofollow/rel/survival checks; splitting per-attribute would duplicate the HTTP + HTML-parsing front-end that all three checks share. Ceiling 320 = round_up_to_10(285+30)."

# CANDIDATE — /ce:plan + /ce:generate + /ce:validate + /ce:publish (4 stages).
# Each stage handler could become its own module; currently stable, tracked.
[files."webui_app/routes/pipeline.py"]
ceiling = 450
rationale = "Audit CANDIDATE (2026-06-15): pipeline stage routes (/ce:plan, /ce:generate, /ce:validate, /ce:publish). radon SLOC 418 across 4 stage handlers + shared helpers. Each stage could split into its own module mirroring the cli/ package split; currently stable, tracked candidate. Ceiling 450 = round_up_to_10(418+30)."

# KEEP — report-anchors formatting. Aggregation + alarm helpers extracted here
# from the CLI; the report engine lives in _report_engine.py already.
[files."src/backlink_publisher/cli/_report_format.py"]
ceiling = 400
rationale = "Audit KEEP (2026-06-15): report-anchors formatting/aggregation/alarm helpers. radon SLOC 363 extracted from the CLI; the pure report engine already lives in _report_engine.py. Remaining size is the formatting + breach-alarm layer that the CLI shell composes. Ceiling 400 = round_up_to_10(363+30)."

# KEEP — PipelineAPI structured wrapper. 3 classes wrapping CLI invocation;
# the plan/validate/publish orchestration is one cohesive in-process API.
[files."webui_app/api/pipeline_api.py"]
ceiling = 320
rationale = "Audit KEEP (2026-06-15): PipelineAPI structured wrapper around plan/validate/publish CLI invocation. radon SLOC 284 across 3 classes; the in-process API surface is one cohesive unit that the WebUI routes call. Ceiling 320 = round_up_to_10(284+30)."

# KEEP — credential provider. Load/probe/refresh share the channel-session
# lifecycle; splitting would spread the provider contract session/ relies on.
[files."src/backlink_publisher/publishing/session/provider.py"]
ceiling = 390
rationale = "Audit KEEP (2026-06-15): credential loading/probing/refreshing for channel sessions. radon SLOC 357 is one cohesive provider contract that session/session_manager.py and the adapters rely on; splitting load from refresh would fragment the credential lifecycle. Ceiling 390 = round_up_to_10(357+30)."

# KEEP — CampaignSqliteStore. CRUD + migration + listing on one store schema.
[files."webui_store/campaign_store.py"]
ceiling = 340
rationale = "Audit KEEP (2026-06-15): CampaignSqliteStore. radon SLOC 307 holds CRUD + migrate_from_json + list/get/update_status on one campaign schema; splitting would fragment the SqliteStore base contract. Ceiling 340 = round_up_to_10(307+30)."

# KEEP — channel status store. 24 fns on one binding-status schema (the badge
# state machine: bound/expired/unbound); splitting would spread the state model.
[files."webui_store/channel_status.py"]
ceiling = 340
rationale = "Audit KEEP (2026-06-15): channel binding-status store. radon SLOC 310 across 24 fns on one status schema (the bound/expired/unbound badge state machine). Splitting would fragment the state model the settings UI + AuthExpiredError flip both read and write. Ceiling 340 = round_up_to_10(310+30)."
